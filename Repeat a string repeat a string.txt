function repeat(str, num) {
  // repeat after me
  var strArr=[];
  var st="";
  
  if(num>0){
  
    for(var i=0;i<num;i++){
    
      strArr.push(str);
      
    
    
  }

}else if(num<0){
      
      return st;
    }
  var x=strArr.join('');
 return x;
}

repeat('x', 3);
